<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/normal"
        app:showAsAction="never"
        android:title="Normal"/>

    <item android:id="@+id/emboss"
        app:showAsAction="never"
        android:title="Emboss"/>

    <item android:id="@+id/blur"
        app:showAsAction="never"
        android:title="Blur"/>

    <item android:id="@+id/clear"
        app:showAsAction="never"
        android:title="Clear"/>

    <item android:title="Save and Share"
        app:showAsAction="never"
        android:id="@+id/share"/>

    <item android:id="@+id/save"
        app:showAsAction="never"
        android:title="Save"/>

    <item android:id="@+id/color"
        app:showAsAction="never"
        android:title="Colors"/>

    <item android:id="@+id/backgroundColor"
        app:showAsAction="never"
        android:title="Background Color"/>

    <item android:id="@+id/eraser"
        app:showAsAction="never"
        android:title="Eraser"/>

    <item android:id="@+id/strokeWidth"
        app:showAsAction="never"
        android:title="Stroke Width"/>

</menu>